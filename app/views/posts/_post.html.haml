%p= post.content
%strong= distance_of_time_in_words_to_now(post.created_at)
%em= link_to 'Edit', edit_post_path(post)
|
%em= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' }